apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hedera-services.fullname" . }}-test-connection"
  labels:
    {{- include "hedera-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hedera-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

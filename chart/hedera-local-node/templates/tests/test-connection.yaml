apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hedera-mirror-node.fullname" . }}-test-connection"
  labels:
    {{- include "hedera-mirror-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hedera-mirror-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

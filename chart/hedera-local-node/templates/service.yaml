apiVersion: v1
kind: Service
metadata:
  name: {{ include "hedera-mirror-node.fullname" . }}
  labels:
    {{- include "hedera-mirror-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hedera-mirror-node.selectorLabels" . | nindent 4 }}
